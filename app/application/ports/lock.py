"""Порт короткоживущего взаимоисключающего лока (DIP).

Защита от гонок/двойных кликов: например, пользователь на плохом инете жмёт «Көру»
много раз — лок гарантирует одну отправку видео. Лок держится TTL-секунды и
снимается САМ (страховка от «зависшего» лока при падении процесса), явного release
нет — это осознанно (для анти-двойного-клика ключ должен пережить саму операцию).

Реализация — `infrastructure/cache/lock.py` (Redis `SET key val EX ttl NX`).
"""

from __future__ import annotations

from typing import Protocol


class Lock(Protocol):
    async def acquire(self, key: str, ttl_seconds: int) -> bool:
        """Атомарно пытается взять лок `key` на `ttl_seconds`.

        True  — лок взят (ключа не было), вызывающий продолжает.
        False — лок уже держит другой запрос, вызывающий тихо выходит.
        """
        ...
