"""Контент публичного канала: ЧТО постим (виды, темы, элемент), КОГДА (сетка слотов) и
КАК выглядит текст (рендереры). Чистый домен — ни Telegram, ни БД.

Смотри PLAN.md §4 «Ведение канала». Ключевая идея: вид (`kind`) — это ФОРМА поста
(квиз с кнопками, квиз с открытым ответом, длинный текст, цитата, список терминов),
а тема (`topic`) — данные-справочник. Новая тема = строка в `topics.py`; новая форма =
редкое событие и класс-рендерер в `render/`.
"""
